apply plugin: 'elasticsearch.internal-es-plugin'
apply plugin: 'elasticsearch.internal-cluster-test'

esplugin {
  name = 'x-pack-kibana-security'
  description = 'Implicit security privileges for Kibana as-data features'
  classname = 'org.elasticsearch.xpack.kibanasecurity.KibanaSecurityPlugin'
  extendedPlugins = ['x-pack-core', 'x-pack-security']
}

base {
  archivesName = 'x-pack-kibana-security'
}

dependencies {
  compileOnly project(path: xpackModule('core'))
  testImplementation(testArtifact(project(xpackModule('core'))))
}

addQaCheckDependencies(project)
